# ===========================================
# Local Proxy Configuration (Claude Max)
# ===========================================
# Use with claude-code-bridge (https://github.com/tsilva/claude-code-bridge)
# to leverage reduced token pricing with a Claude Max subscription.
#
# Usage: python extract.py --profile <name> --env local

# Local proxy URL (claude-code-bridge default port)
OPENROUTER_BASE_URL=http://127.0.0.1:8000/v1

# Model configuration (Claude models recommended for Max subscription)
EXTRACT_MODEL_ID=anthropic/claude-opus-4.5
SELF_CONSISTENCY_MODEL_ID=anthropic/claude-opus-4.5

# Optional: Override worker count for local testing
MAX_WORKERS=10
