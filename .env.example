# ===========================================
# Core Extraction Settings
# ===========================================

# Model for self-consistency voting (when N_EXTRACTIONS > 1)
SELF_CONSISTENCY_MODEL_ID=google/gemini-2.5-flash

# Primary vision model for extraction
# Options by provider (updated January 2026):
#   Anthropic: anthropic/claude-opus-4.5 ($5/M in), anthropic/claude-sonnet-4, anthropic/claude-haiku-4.5
#   Google:    google/gemini-3-flash-preview ($0.50/M in), google/gemini-2.5-flash ($0.30/M in)
#   OpenAI:    openai/gpt-5.2 ($1.75/M in), openai/gpt-4.1, openai/gpt-4.1-mini
#   Qwen:      qwen/qwen3-max ($1.20/M in), qwen/qwen3-vl-32b-instruct, qwen/qwen3-vl-8b-instruct
EXTRACT_MODEL_ID=google/gemini-2.5-flash

# Input/Output paths
INPUT_PATH=<path to the folder containing the PDF files>
INPUT_FILE_REGEX=<regex to match the PDF files, e.g., .*\.pdf>
OUTPUT_PATH=<path to the folder where the output files will be saved>

# OpenRouter API key (get one at https://openrouter.ai/keys)
OPENROUTER_API_KEY=<your API key>

# Number of extraction attempts for self-consistency (1 = no self-consistency)
N_EXTRACTIONS=3

# Parallel workers for PDF processing
MAX_WORKERS=1

# ===========================================
# Post-Extraction Verification Settings
# ===========================================

# Enable/disable multi-model verification pipeline (default: true)
# When enabled, extractions are verified against a different model
ENABLE_VERIFICATION=true

# Override automatic verification model selection (optional)
# By default, a model from a DIFFERENT provider than EXTRACT_MODEL_ID is chosen
# Example: If EXTRACT_MODEL_ID is google/*, verification uses anthropic/*
# Options (updated January 2026):
#   anthropic/claude-opus-4.5      - Frontier reasoning model ($5/M in, $25/M out)
#   anthropic/claude-sonnet-4      - Good balance of speed/quality
#   google/gemini-3-flash-preview  - Latest Google model ($0.50/M in, $3/M out)
#   google/gemini-2.5-flash        - Stable workhorse ($0.30/M in, $2.50/M out)
#   openai/gpt-5.2                 - Latest OpenAI model ($1.75/M in, $14/M out)
#   qwen/qwen3-max                 - SOTA Qwen model ($1.20/M in, $6/M out)
# VERIFICATION_MODEL_ID=anthropic/claude-opus-4.5

# Override automatic arbitration model selection (optional)
# Used to resolve disagreements between primary and verification models
# By default, uses a model from a THIRD provider
# ARBITRATION_MODEL_ID=openai/gpt-5.2

# Check for results missed by primary extraction (default: true)
ENABLE_COMPLETENESS_CHECK=true

# Character-by-character verification for uncertain values (default: true)
ENABLE_CHARACTER_VERIFICATION=true
